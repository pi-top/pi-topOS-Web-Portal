#!/bin/bash -e

if [ -z "${2}" ]; then
	if [[ -L /etc/xdg/autostart/pt-os-setup.desktop ]]; then
		rm /etc/xdg/autostart/pt-os-setup.desktop
	fi

	update-alternatives --install /usr/bin/x-session-manager x-session-manager /usr/bin/openbox-session 40
fi

#DEBHELPER#
