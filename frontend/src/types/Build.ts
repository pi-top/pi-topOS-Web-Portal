export type BuildInfo = {
  buildDate: string;
  buildNumber: string;
  buildCommit: string;
  schemaVersion: string;
  buildType: string;
  buildOsVersion: string;
  buildName: string;
  buildRepo: string;
  finalRepo: string;
  ptOsWebPortalVersion: string;
}
